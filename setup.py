from setuptools import setup, find_packages

setup(
    name='football-viz',
    version='0.1.0',
    py_modules=['football_viz'],
    install_requires=[
        'matplotlib',
        'adjustText',
    ],
)
